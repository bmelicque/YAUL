{
  "name": "signals",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild ./src/index.tsx --bundle --minify --outfile=/_dist/out.js --target=es5 --watch",
    "build": "esbuild ./src/signal.tsx --bundle --minify --outfile=/_dist/out.js --target=es5"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "esbuild": "^0.18.6"
  }
}